import totalSteps from './part1';
import nextBiggerValue from './part2';

const target = 325489;
console.log(`Total steps needed is ${totalSteps(target)}`);
console.log(`Next bigger value than ${target} is ${nextBiggerValue(target)}`)